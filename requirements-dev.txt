# Development / test dependencies.
# Includes all runtime deps plus tooling.
# Install with: pip install -r requirements-dev.txt
# Or (recommended) with editable install: pip install -e ".[dev]"
-r requirements.txt
httpx>=0.28.0
pytest>=8.3.0
pytest-asyncio>=0.25.0
ruff>=0.9.0
mypy>=1.14.0
